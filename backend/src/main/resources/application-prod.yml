spring:
  datasource:
    hikari:
      connection-timeout: 210000 # 210초
      maximum-pool-size: 10
    url: jdbc:mysql://172.30.1.89:3306/mydb?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: timetrove
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
  data:
    redis:
      host: redis
      port: 6379
  security:
    oauth2:
      client:
        registration:
          kakao:
            client-id: d008d7a526a3e250c2bd63146f16c578
            client-secret: PyZvv74qu9JZNVHO2chFnpB9uBrqJpj0
            redirect-uri: http://ec2-3-38-77-252.ap-northeast-2.compute.amazonaws.com/api/login/oauth2/callback/kakao
            scope:
              - profile_nickname
              - profile_image
            client-name: Kakao
            authorization-grant-type: authorization_code
            client-authentication-method: client_secret_post
        provider:
          kakao:
            authorization-uri: https://kauth.kakao.com/oauth/authorize
            token-uri: https://kauth.kakao.com/oauth/token
            user-info-uri: https://kapi.kakao.com/v2/user/me
            user-name-attribute: id
  web:
    resources:
      static-locations: file:/app/uploads/
logging:
  level:
    org.hibernate.SQL: DEBUG
    org.hibernate.type.descriptor.sql.BasicBinder: TRACE